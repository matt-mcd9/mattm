
int c = 5 / b; //fixed divide by zero exception

d= math.cos(math.pi/3);




int d = sqrt(pow(integral(theta, infinity)));


int d = sqrt(pow(integral(theta, 0)));


int c = 5 / b; //fixed divide by zero exception

d= math.cos(math.pi/3);




int d = sqrt(pow(integral(theta, infinity)));

int d = sqrt(pow(integral(theta, 0)));
